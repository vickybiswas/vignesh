services:

  vignesh-ui:
    volumes:
      - d:/code/vignesh/:/app
    ports:
      - 3000:3000
      - 49153:49153
    build:
      context: ./
    networks:
      - app_network

networks:
  app_network:
    driver: bridge
  